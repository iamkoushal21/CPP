// given - N- so 1 to N number, Alternate deletion of elements starting with 1st position and return last element left with this.

#include <bits/stdc++.h>
using namespace std;

int main() {
  int N;
  cin >> N;
  vector <int> v(N);
  iota(v.begin(), v.begin()+N, 1);
  // for (int i = 0; i < N; i++) {
  //   cout << v[i] << " ";
  // }
  size = N;
  for (i = 0; i < size; i++) {
    if (i % 2 == 0) {
      
    }
  }
  return 0;
}
